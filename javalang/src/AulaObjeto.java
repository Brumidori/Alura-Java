public class AulaObjeto {

    //Todas as classes trabalham com a regra de que a classe Object está no topo da hierarquia, sendo esta a classe mãe.
    // Não é necessário escrever extends Object nas classes filhas como Cliente, pois o compilador insere essa
    // informação automaticamente.

    //A ideia do toString() da classe Object é ser sobrescrito, assim como a maioria dos outros métodos "querem"
    // ser sobrescritos pelos filhos, com isso eles ganham mais significado.
    // É o que fizemos na classe Conta, para procurarmos rapidamente o método, acionamos o atalho "Ctrl + O"
    // e o procuramos dentro da classe e observamos a atribuição de significado.
}
